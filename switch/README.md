# Luna2 Switch Backend for Open OnDemand

Python backend service for managing Luna2 switch configuration from the TrinityX Open OnDemand interface.

This service sits between Open OnDemand and the Luna2 daemon. It exposes a small HTTP API that the future Vue.js frontend can call to list, create, update, clone, rename and delete switch records.

```text
TrinityX
  └── Open OnDemand
        └── Luna2 Switch Python Backend
              ├── Flask API: /api/v1/...
              ├── Token handling for Luna2 daemon
              └── Luna2 daemon REST API
                    └── Switch configuration

Future UI flow:
Open OnDemand page → Vue.js frontend → Python backend → Luna2 daemon
```

---

## 1. Purpose

The backend provides a switch management API for Luna2 `switch` objects inside TrinityX Open OnDemand.

It is responsible for:

- serving the Open OnDemand application shell;
- exposing switch-related API routes for the frontend;
- reading Luna daemon connection details from the Luna configuration file;
- retrieving and caching a Luna daemon API token;
- forwarding switch actions to the Luna2 daemon;
- returning a consistent frontend-friendly response where possible.

Switch records define the switch access and discovery information used by Luna. This commonly includes SNMP object identifiers, read/write community strings, optional MAC address details, vendor information and uplink port metadata.

---

## 2. Application details

| Item | Value |
| --- | --- |
| Application | Luna2 Switch Backend for Open OnDemand |
| Backend framework | Flask |
| API version | `v1` |
| Default development port | `7755` |
| Main table | `switch` |
| Frontend | Vue.js application, planned |
| Backend-to-daemon timeout | 5 seconds |
| Licence | GPL |

Example base URL:

```text
https://vmware-controller1.cluster:7755
```

Example API base path:

```text
https://vmware-controller1.cluster:7755/api/v1
```

---

## 3. Configuration

The backend reads Luna daemon API settings from the Luna configuration file defined by `INI_FILE`.

The expected configuration section is:

```ini
[API]
USERNAME=<luna-api-username>
PASSWORD=<luna-api-password>
SECRET_KEY=<jwt-secret-key>
PROTOCOL=https
ENDPOINT=<luna-daemon-host>:<luna-daemon-port>
VERIFY_CERTIFICATE=true
```

The backend also uses a `TOKEN_FILE` to cache the JWT token received from the Luna2 daemon. The token file is written with permission mode `0600`.

Before processing requests, the backend validates that:

- the Luna configuration file exists;
- the Luna configuration file is readable;
- the token file path is valid.

If this validation fails, the backend returns the Open OnDemand `error.html` template rather than a JSON API response.

---

## 4. Runtime modes

When `APP_STATE` is `False`, the backend runs in development mode.

Development mode enables:

- Flask debug mode;
- CORS for `http://localhost:5173`;
- direct Flask serving on `0.0.0.0:7755`;
- optional SSL if the configured certificate and key files exist.

Development SSL paths used by the current code:

```text
/trinity/local/etc/ssl/vmware-controller1.cluster.crt
/trinity/local/etc/ssl/vmware-controller1.cluster.key
```

When `APP_STATE` is not `False`, the application starts with the default Flask `app.run()` behaviour and is expected to be served in the production Open OnDemand context.

---

## 5. General response format

Most API endpoints return the following JSON envelope:

```json
{
  "status": true,
  "status_code": 200,
  "content": {}
}
```

| Field | Meaning |
| --- | --- |
| `status` | Boolean success indicator generated by the backend wrapper. |
| `status_code` | Luna daemon status code or backend-generated status code. |
| `content` | Data or error details returned by the Luna daemon or backend. |

Important frontend note:

The Flask route itself often returns HTTP `200`, even when the JSON body contains an error such as `status: false` and `status_code: 404`.

For the Vue.js frontend, do not rely only on the HTTP response code. Always check:

```js
if (response.data.status === true) {
  // show success box
} else {
  // show failure box using response.data.content
}
```

Exceptions:

- `GET /` returns HTML.
- `GET /api/v1/license` returns text/HTML, not the standard JSON envelope.
- configuration validation errors return `error.html`.
- unknown routes return `error.html` with HTTP `200`.

---

## 6. Switch object structure

A switch record is expected under:

```json
{
  "config": {
    "switch": {
      "<switch-name>": {
        "name": "<switch-name>",
        "oid": "<snmp-oid>",
        "read": "<snmp-read-community>",
        "rw": "<snmp-read-write-community>",
        "vendor": "<optional-vendor>",
        "macaddress": "<optional-mac-address>",
        "uplinkports": "<optional-uplink-ports>",
        "comment": "<optional-comment>"
      }
    }
  }
}
```

Example switch object:

```json
{
  "config": {
    "switch": {
      "switch001": {
        "comment": null,
        "macaddress": null,
        "name": "switch001",
        "oid": ".1.3.6.1.2.1.17.7.1.2.2.1.2",
        "read": "public",
        "rw": "<masked-rw-community>",
        "uplinkports": null,
        "vendor": null
      }
    }
  }
}
```

The backend uses the first key below `config.switch` as the record name:

```python
name = next(iter(request_data["config"]["switch"]))
```

For predictable behaviour, send only one switch record per create, update, clone or rename request.

### Field notes

| Field | Type | Notes |
| --- | --- | --- |
| `name` | String | Switch name. Should match the object key for add/update operations. |
| `oid` | String | SNMP OID used by Luna for switch discovery or MAC table lookup. |
| `read` | String | SNMP read community. Treat as sensitive configuration data. |
| `rw` | String | SNMP read/write community. Treat as sensitive and mask in the UI. |
| `uplinkports` | String/list/null | Optional uplink port metadata, depending on daemon-side expectations. |
| `vendor` | String/null | Optional switch vendor name. |
| `macaddress` | String/null | Optional switch MAC address. |
| `comment` | String/null | Optional operator note. |

Security note: SNMP community strings are sensitive, especially the read/write community. The frontend should avoid displaying `rw` in clear text and should not log submitted payloads containing SNMP credentials.

---

# 7. API reference

## 7.1 List available routes

```http
GET /api/v1/routes
```

Returns all available backend routes with route URL, Flask function name and HTTP method.

### Success message box

```text
Routes loaded successfully.
```

### Failure message box

```text
Could not load backend routes. Check whether the Luna configuration file and token file are available.
```

### Success response example

```json
[
  {
    "function": "routes",
    "method": "GET",
    "route": "https://vmware-controller1.cluster:7755/api/v1/routes"
  },
  {
    "function": "switches",
    "method": "GET",
    "route": "https://vmware-controller1.cluster:7755/api/v1/switches"
  }
]
```

### Failure response example

This endpoint can return the `error.html` page if pre-request validation fails.

Common reasons:

- Luna configuration file is missing;
- Luna configuration file is not readable;
- token file path is invalid.

---

## 7.2 Open application shell

```http
GET /
```

Serves the Open OnDemand application shell. The backend injects `APP_URL` into the template so the frontend can understand its current base URL.

### Success message box

```text
Switch application loaded successfully.
```

### Failure message box

```text
Could not load the Switch application. Check the backend configuration and Open OnDemand app path.
```

### Success response example

```html
<!-- index.html rendered by Flask -->
```

### Failure response example

```html
<!-- error.html rendered by Flask -->
```

---

## 7.3 List switches

```http
GET /api/v1/switches
```

Returns all switch records available from the Luna2 daemon.

Internally calls:

```text
GET <luna-daemon>/config/switch
```

Note: the current route is `/switches` because that is the function/route name used in the backend. The UI can display the label as “Switches”.

### Success message box

```text
Switches loaded successfully.
```

### Failure message box

```text
Could not load switches. Check the Luna daemon connection and API credentials.
```

### Success response example

```json
{
  "content": {
    "config": {
      "switch": {
        "switch001": {
          "comment": null,
          "macaddress": null,
          "name": "switch001",
          "oid": ".1.3.6.1.2.1.17.7.1.2.2.1.2",
          "read": "public",
          "rw": "<masked-rw-community>",
          "uplinkports": null,
          "vendor": null
        },
        "switch02": {
          "comment": null,
          "macaddress": null,
          "name": "switch02",
          "oid": ".1.3.6.1.2.1.17.7.1.2.2.1.2",
          "read": "public",
          "rw": "<masked-rw-community>",
          "uplinkports": null,
          "vendor": null
        }
      }
    }
  },
  "status": true,
  "status_code": 200
}
```

### Failure response example

```json
{
  "status": false,
  "status_code": 400,
  "content": "Request Timeout while https://<luna-daemon>/config/switch"
}
```

---

## 7.4 Add switch

```http
POST /api/v1/add
```

Creates a new switch record in Luna2.

Internally calls:

```text
POST <luna-daemon>/config/switch/<switch-name>
```

The backend derives `<switch-name>` from the first key below `config.switch`.

### Success message box

```text
Switch added successfully.
```

### Failure message box

```text
Could not add switch. Check the switch name, SNMP settings and Luna daemon validation message.
```

### Request body example

```json
{
  "config": {
    "switch": {
      "new-switch": {
        "comment": null,
        "macaddress": null,
        "name": "new-switch",
        "oid": ".1.3.6.1.2.1.17.7.1.2.2.1.2",
        "read": "public",
        "rw": "<rw-community>",
        "uplinkports": null,
        "vendor": null
      }
    }
  }
}
```

### Success response example

```json
{
  "status": true,
  "status_code": 201,
  "content": {
    "config": {
      "switch": {
        "new-switch": {
          "name": "new-switch",
          "oid": ".1.3.6.1.2.1.17.7.1.2.2.1.2",
          "read": "public",
          "rw": "<masked-rw-community>"
        }
      }
    }
  }
}
```

### Failure response examples

No JSON payload:

```json
{
  "status": false,
  "status_code": 400,
  "message": "No JSON payload received"
}
```

Daemon validation error:

```json
{
  "status": false,
  "status_code": 400,
  "content": {
    "message": "Invalid request: switch new-switch could not be created"
  }
}
```

---

## 7.5 Update switch

```http
PUT /api/v1/update
```

Updates an existing switch record in Luna2.

Internally calls:

```text
POST <luna-daemon>/config/switch/<switch-name>
```

The backend expects the Luna daemon to return HTTP `204` for a successful update.

### Success message box

```text
Switch updated successfully.
```

### Failure message box

```text
Could not update switch. Check whether the switch exists and whether the submitted values are valid.
```

### Request body example

```json
{
  "config": {
    "switch": {
      "switch001": {
        "comment": "Updated SNMP settings",
        "macaddress": null,
        "name": "switch001",
        "oid": ".1.3.6.1.2.1.17.7.1.2.2.1.2",
        "read": "public",
        "rw": "<updated-rw-community>",
        "uplinkports": null,
        "vendor": "generic"
      }
    }
  }
}
```

### Success response example

```json
{
  "status": true,
  "status_code": 204,
  "content": {
    "message": "Switch switch001 is updated successfully."
  }
}
```

### Failure response examples

No JSON payload:

```json
{
  "status": false,
  "status_code": 400,
  "message": "No JSON payload received"
}
```

Daemon validation error:

```json
{
  "status": false,
  "status_code": 404,
  "content": {
    "message": "Invalid request: switch switch001 does not exist"
  }
}
```

---

## 7.6 Clone switch

```http
POST /api/v1/clone
```

Clones an existing switch record in Luna2.

Internally calls:

```text
POST <luna-daemon>/config/switch/<switch-name>
```

The backend derives `<switch-name>` from the first key below `config.switch` and sends the payload to the daemon with `action="clone"`.

The exact payload required by the Luna daemon depends on daemon-side clone semantics. Based on the rename pattern, the payload should contain the source switch and a daemon-recognised field for the target clone name.

### Success message box

```text
Switch cloned successfully.
```

### Failure message box

```text
Could not clone switch. Check the source switch, target name and daemon validation message.
```

### Request body example

Use the field expected by the Luna daemon for the new clone name. If the daemon uses `newswitchname`, the payload may look like this:

```json
{
  "config": {
    "switch": {
      "switch001": {
        "newswitchname": "switch001-copy"
      }
    }
  }
}
```

If the daemon expects a full target object, use this shape instead:

```json
{
  "config": {
    "switch": {
      "switch001-copy": {
        "comment": "Cloned from switch001",
        "macaddress": null,
        "name": "switch001-copy",
        "oid": ".1.3.6.1.2.1.17.7.1.2.2.1.2",
        "read": "public",
        "rw": "<copied-or-new-rw-community>",
        "uplinkports": null,
        "vendor": null
      }
    }
  }
}
```

### Success response example

The expected ideal backend response should be:

```json
{
  "status": true,
  "status_code": 201,
  "content": {
    "message": "Switch switch001 was cloned successfully."
  }
}
```

### Failure response examples

No JSON payload:

```json
{
  "status": false,
  "status_code": 400,
  "message": "No JSON payload received"
}
```

Daemon validation error:

```json
{
  "status": false,
  "status_code": 400,
  "content": {
    "message": "Invalid request: switch switch001 could not be cloned"
  }
}
```

### Backend implementation caveat

The application route exposes `POST /api/v1/clone`, but the `Rest.post_data()` implementation must explicitly handle `action == "clone"`.

If the `Rest.post_data()` logic is still the same as the Network backend version, clone success may be reported incorrectly because only these actions currently have explicit success handling:

- `add` with daemon HTTP `201`;
- `update` with daemon HTTP `204`;
- `rename` with daemon HTTP `204`.

Recommended backend addition:

```python
elif response.status_code in [200, 201, 204] and action == "clone":
    self.response = {
        "status": True,
        "status_code": 200 if response.status_code == 204 else response.status_code,
        "content": {"message": f"{table.capitalize()} {name} is cloned successfully."}
    }
```

If the daemon returns a JSON body on clone, preserve that JSON in `content` instead of replacing it with a generic message.

---

## 7.7 Rename switch

```http
PATCH /api/v1/rename
```

Renames an existing switch record.

Internally calls:

```text
POST <luna-daemon>/config/switch/<old-switch-name>
```

The old name is taken from the first key below `config.switch`.

The new name must be provided as `newswitchname` inside the old switch object.

### Success message box

```text
Switch renamed successfully.
```

### Failure message box

```text
Could not rename switch. Provide both the current switch name and the new switch name.
```

### Request body example

```json
{
  "config": {
    "switch": {
      "switch001": {
        "newswitchname": "renamed-switch"
      }
    }
  }
}
```

### Success response example

```json
{
  "status": true,
  "status_code": 200,
  "content": {
    "message": "Switch switch001 is renamed successfully."
  }
}
```

### Failure response examples

No JSON payload:

```json
{
  "status": false,
  "message": "No JSON payload received"
}
```

Missing current or new name:

```json
{
  "status": false,
  "status_code": 400,
  "content": "ERROR :: Record name and new name must be provided."
}
```

Daemon validation error:

```json
{
  "status": false,
  "status_code": 404,
  "content": {
    "message": "Invalid request: switch switch001 does not exist"
  }
}
```

---

## 7.8 Delete switch

```http
DELETE /api/v1/delete/<record>
```

Deletes a switch record from Luna2.

Internally calls:

```text
GET <luna-daemon>/config/switch/<record>/_delete
```

### Path parameters

| Parameter | Required | Description |
| --- | --- | --- |
| `record` | Yes | Switch name to delete. |

### Success message box

```text
Switch deleted successfully.
```

### Failure message box

```text
Could not delete switch. Check whether the switch exists or is still referenced by other Luna objects.
```

### Success response example

```json
{
  "status": true,
  "status_code": 204,
  "content": {
    "message": "Switch switch001 is deleted successfully."
  }
}
```

### Failure response example

```json
{
  "status": false,
  "status_code": 404,
  "content": {
    "message": "Invalid request: switch switch001 does not exist"
  }
}
```

---

## 7.9 Licence information

```http
GET /api/v1/license
```

Returns the application licence text if the licence file exists and is readable.

Unlike most other API routes, this endpoint returns a plain text or HTML string, not the standard JSON envelope.

### Success message box

```text
Licence information loaded successfully.
```

### Failure message box

```text
Licence information is not available at this moment.
```

### Success response example

```html
GNU GENERAL PUBLIC LICENSE<br />Version 3, 29 June 2007<br />...
```

### Failure response example

```text
LICENSE Information is not available at this moment.
```

---

# 8. Frontend handling guide

## 8.1 Standard JSON API handling

For most API endpoints, the Vue.js frontend should check the JSON body:

```js
const result = await apiCall()

if (result.data?.status === true) {
  showSuccess(result.data.content)
} else {
  showFailure(result.data?.content || 'Request failed')
}
```

Do not treat HTTP `200` as success by itself.

The backend may return:

```json
{
  "status": false,
  "status_code": 404,
  "content": {
    "message": "Invalid request: switch missing-switch does not exist"
  }
}
```

with an outer Flask HTTP response of `200`.

## 8.2 Error message extraction

Recommended frontend message extraction:

```js
function getApiMessage(data, fallback = 'Request failed') {
  if (!data) return fallback

  if (typeof data.content === 'string') return data.content

  if (data.content?.message) return data.content.message

  if (data.message) return data.message

  return fallback
}
```

## 8.3 SNMP credential handling in the UI

Switch objects may contain SNMP community strings. Recommended frontend behaviour:

- mask `rw` values by default;
- avoid displaying SNMP read/write community strings in table views;
- avoid writing request payloads containing SNMP credentials into browser logs;
- show only whether a read/write community is configured, unless the user is editing it;
- only send credential updates when the user intentionally changes the field, if the daemon supports partial updates.

## 8.4 Suggested UI behaviour

| Action | After success | After failure |
| --- | --- | --- |
| List switches | Render table/cards with sensitive fields masked | Show error box and retry button |
| Add switch | Show success box, refresh switch list | Keep form values and show daemon message |
| Update switch | Show success box, refresh selected switch | Keep edit form open |
| Clone switch | Show success box, refresh switch list | Keep clone dialog open |
| Rename switch | Show success box, refresh switch list | Keep rename dialog open |
| Delete switch | Show success box, refresh switch list | Keep item visible and show error |
| Licence | Render licence text | Show fallback message |

---

# 9. cURL examples

## List routes

```bash
curl -k https://vmware-controller1.cluster:7755/api/v1/routes
```

## List switches

```bash
curl -k https://vmware-controller1.cluster:7755/api/v1/switches
```

## Add switch

```bash
curl -k -X POST https://vmware-controller1.cluster:7755/api/v1/add \
  -H 'Content-Type: application/json' \
  -d '{
    "config": {
      "switch": {
        "new-switch": {
          "comment": null,
          "macaddress": null,
          "name": "new-switch",
          "oid": ".1.3.6.1.2.1.17.7.1.2.2.1.2",
          "read": "public",
          "rw": "<rw-community>",
          "uplinkports": null,
          "vendor": null
        }
      }
    }
  }'
```

## Update switch

```bash
curl -k -X PUT https://vmware-controller1.cluster:7755/api/v1/update \
  -H 'Content-Type: application/json' \
  -d '{
    "config": {
      "switch": {
        "switch001": {
          "comment": "Updated SNMP settings",
          "macaddress": null,
          "name": "switch001",
          "oid": ".1.3.6.1.2.1.17.7.1.2.2.1.2",
          "read": "public",
          "rw": "<updated-rw-community>",
          "uplinkports": null,
          "vendor": "generic"
        }
      }
    }
  }'
```

## Clone switch

```bash
curl -k -X POST https://vmware-controller1.cluster:7755/api/v1/clone \
  -H 'Content-Type: application/json' \
  -d '{
    "config": {
      "switch": {
        "switch001": {
          "newswitchname": "switch001-copy"
        }
      }
    }
  }'
```

## Rename switch

```bash
curl -k -X PATCH https://vmware-controller1.cluster:7755/api/v1/rename \
  -H 'Content-Type: application/json' \
  -d '{
    "config": {
      "switch": {
        "switch001": {
          "newswitchname": "renamed-switch"
        }
      }
    }
  }'
```

## Delete switch

```bash
curl -k -X DELETE https://vmware-controller1.cluster:7755/api/v1/delete/renamed-switch
```

## Get licence

```bash
curl -k https://vmware-controller1.cluster:7755/api/v1/license
```

---

# 10. Backend behaviour notes

These details matter for frontend implementation.

1. The backend is a wrapper around the Luna2 daemon. It does not store switch data itself.
2. The backend retrieves daemon credentials from the Luna configuration file.
3. The backend requests a Luna daemon token and caches it in `TOKEN_FILE`.
4. If the cached token is invalid or expired, the backend requests a new token.
5. Create, update, clone and rename requests all call the Luna daemon with `POST` internally.
6. Delete calls the Luna daemon using the daemon-specific `_delete` route.
7. The route method exposed to the frontend may differ from the method used internally against the Luna daemon.
8. Most API routes return JSON, but the application shell, licence route and configuration errors return HTML/text.
9. The frontend should refresh `/api/v1/switches` after add, update, clone, rename or delete actions.
10. The frontend should show daemon validation messages directly where possible because they explain the real reason for failure.
11. SNMP community strings should be handled as sensitive configuration data in the UI and logs.

---

# 11. Recommended frontend success and failure components

Every API action should expose two visible message areas.

## Success box

Use this for completed actions.

```text
Title: Success
Body: <action-specific success message>
Style: green/positive state
Behaviour: auto-dismiss after a few seconds, except for destructive actions where keeping it visible can help confirmation
```

## Failure box

Use this for validation, daemon, configuration or connection errors.

```text
Title: Request failed
Body: <daemon message or backend fallback message>
Style: red/error state
Behaviour: keep visible until the user closes it or retries
```

The failure box should prefer messages in this order:

1. `content.message`
2. `content` when it is a string
3. `message`
4. generic fallback message

---

# 12. Current API route overview

| Method | Route | Purpose |
| --- | --- | --- |
| `GET` | `/` | Serve Open OnDemand application shell |
| `GET` | `/api/v1/routes` | List available backend routes |
| `GET` | `/api/v1/switches` | List all Luna2 switches |
| `POST` | `/api/v1/add` | Add a switch |
| `PUT` | `/api/v1/update` | Update a switch |
| `POST` | `/api/v1/clone` | Clone a switch |
| `PATCH` | `/api/v1/rename` | Rename a switch |
| `DELETE` | `/api/v1/delete/<record>` | Delete a switch |
| `GET` | `/api/v1/license` | Return licence information |

---

# 13. Known limitations and improvement points

These are not blockers, but they are useful to know before connecting the Vue.js frontend.

- The list route is currently named `/api/v1/switches`; the UI can still label the objects as “Switches”.
- Some error responses are JSON, while others are HTML/text.
- Some Flask routes return HTTP `200` even when the body contains an error status.
- `rename` returns a missing-payload error without `status_code`.
- `404` pages are rendered as `error.html` with HTTP `200`.
- Create, update, clone and rename assume one switch object per request.
- The backend currently trusts the payload shape and passes validation mainly to the Luna daemon.
- If the `Rest.post_data()` implementation has not been updated for `action == "clone"`, the clone route may not return a correct success response even when the daemon accepts the request.
- SNMP community strings are returned in the current `/api/v1/switches` response. The frontend should mask sensitive values, and the backend may later consider redacting `rw` before sending data to the browser.

For a cleaner frontend contract later, consider standardising all API errors as JSON under the same envelope:

```json
{
  "status": false,
  "status_code": 400,
  "content": {
    "message": "Human-readable error message"
  }
}
```

A cleaner display name helper can keep UI/backend messages readable:

```python
DISPLAY_NAME = "Switch"
```

For future cleanup, the route could also be renamed from `/switches` to `/switches`, while keeping `/switches` as a backwards-compatible alias.

---

# 14. Licence

This backend is part of the TrinityX software suite and is distributed under the GNU General Public License.

See the project licence file for full licence terms.
