# Luna2 Network Backend for Open OnDemand

Python backend service for managing Luna2 network configuration from the TrinityX Open OnDemand interface.

This service sits between Open OnDemand and the Luna2 daemon. It exposes a small HTTP API that the future Vue.js frontend can call to list, create, update, rename, delete and inspect networks.

```text
TrinityX
  └── Open OnDemand
        └── Luna2 Network Python Backend
              ├── Flask API: /api/v1/...
              ├── Token handling for Luna2 daemon
              └── Luna2 daemon REST API
                    └── Network configuration

Future UI flow:
Open OnDemand page → Vue.js frontend → Python backend → Luna2 daemon
```

---

## Installation
This package requires `python3.10` and the python modules listed in the `requirements.txt` file
```
pip install -r requirements.txt
```

## Integration
In order to fully integrate this app inside OOD the following steps are required:
- Install `python3` and `python3-pip`
- Clone the repository to `/var/www/ood/apps/sys/`
- Install the python modules located inside `requirements.txt`

## 1. Purpose

The backend provides a network-management API for Luna2 network objects inside TrinityX Open OnDemand.

It is responsible for:

- serving the Open OnDemand application shell;
- exposing network-related API routes for the frontend;
- reading Luna daemon connection details from the Luna configuration file;
- retrieving and caching a Luna daemon API token;
- forwarding network actions to the Luna2 daemon;
- returning a consistent frontend-friendly response where possible.

---

## 2. Application details

| Item | Value |
| --- | --- |
| Application | Luna2 Network Backend for Open OnDemand |
| Backend framework | Flask |
| API version | `v1` |
| Default development port | `7755` |
| Main table | `network` |
| Frontend | Vue.js application, planned |
| Backend-to-daemon timeout | 5 seconds |
| Licence | GPL |

Example base URL:

```text
https://vmware-controller1.cluster:7755
```

Example API base path:

```text
https://vmware-controller1.cluster:7755/api/v1
```

---

## 3. Configuration

The backend reads Luna daemon API settings from the Luna configuration file defined by `INI_FILE`.

The expected configuration section is:

```ini
[API]
USERNAME=<luna-api-username>
PASSWORD=<luna-api-password>
SECRET_KEY=<jwt-secret-key>
PROTOCOL=https
ENDPOINT=<luna-daemon-host>:<luna-daemon-port>
VERIFY_CERTIFICATE=true
```

The backend also uses a `TOKEN_FILE` to cache the JWT token received from the Luna2 daemon. The token file is written with permission mode `0600`.

Before processing requests, the backend validates that:

- the Luna configuration file exists;
- the Luna configuration file is readable;
- the token file path is valid.

If this validation fails, the backend returns the Open OnDemand `error.html` template rather than a JSON API response.

---

## 4. Runtime modes

When `APP_STATE` is `False`, the backend runs in development mode.

Development mode enables:

- Flask debug mode;
- CORS for `http://localhost:5173`;
- direct Flask serving on `0.0.0.0:7755`;
- optional SSL if the configured certificate and key files exist.

Development SSL paths used by the current code:

```text
/trinity/local/etc/ssl/vmware-controller1.cluster.crt
/trinity/local/etc/ssl/vmware-controller1.cluster.key
```

When `APP_STATE` is not `False`, the application starts with the default Flask `app.run()` behaviour and is expected to be served in the production Open OnDemand context.

---

## 5. General response format

Most API endpoints return the following JSON envelope:

```json
{
  "status": true,
  "status_code": 200,
  "content": {}
}
```

| Field | Meaning |
| --- | --- |
| `status` | Boolean success indicator generated by the backend wrapper. |
| `status_code` | Luna daemon status code or backend-generated status code. |
| `content` | Data or error details returned by the Luna daemon or backend. |

Important frontend note:

The Flask route itself often returns HTTP `200`, even when the JSON body contains an error such as `status: false` and `status_code: 404`.

For the Vue.js frontend, do not rely only on the HTTP response code. Always check:

```js
if (response.data.status === true) {
  // show success box
} else {
  // show failure box using response.data.content
}
```

Exceptions:

- `GET /` returns HTML.
- `GET /api/v1/license` returns text/HTML, not the standard JSON envelope.
- configuration validation errors return `error.html`.
- unknown routes return `error.html` with HTTP `200`.

---

## 6. Network object structure

A network record is expected under:

```json
{
  "config": {
    "network": {
      "<network-name>": {
        "name": "<network-name>",
        "network": "<cidr>",
        "type": "ethernet",
        "zone": "internal"
      }
    }
  }
}
```

Example network object:

```json
{
  "config": {
    "network": {
      "cluster": {
        "comment": null,
        "dhcp": true,
        "dhcp_nodes_in_pool": false,
        "dhcp_nodes_only": false,
        "dhcp_range_begin": "10.141.128.0",
        "dhcp_range_begin_ipv6": null,
        "dhcp_range_end": "10.141.140.0",
        "dhcp_range_end_ipv6": null,
        "gateway": "10.141.255.254",
        "gateway_ipv6": null,
        "gateway_metric": 101,
        "name": "cluster",
        "nameserver_ip": null,
        "nameserver_ip_ipv6": null,
        "network": "10.141.0.0/16",
        "network_ipv6": null,
        "non_authoritative": null,
        "ntp_server": null,
        "shared": null,
        "subnet_ipv6": null,
        "type": "ethernet",
        "zone": "internal"
      }
    }
  }
}
```

The backend uses the first key below `config.network` as the record name:

```python
name = next(iter(request_data["config"]["network"]))
```

For predictable behaviour, send only one network record per create, update or rename request.

---

# 7. API reference

## 7.1 List available routes

```http
GET /api/v1/routes
```

Returns all available backend routes with route URL, Flask function name and HTTP method.

### Success message box

```text
Routes loaded successfully.
```

### Failure message box

```text
Could not load backend routes. Check whether the Luna configuration file and token file are available.
```

### Success response example

```json
[
  {
    "function": "routes",
    "method": "GET",
    "route": "https://vmware-controller1.cluster:7755/api/v1/routes"
  },
  {
    "function": "networks",
    "method": "GET",
    "route": "https://vmware-controller1.cluster:7755/api/v1/networks"
  }
]
```

### Failure response example

This endpoint can return the `error.html` page if pre-request validation fails.

Common reasons:

- Luna configuration file is missing;
- Luna configuration file is not readable;
- token file path is invalid.

---

## 7.2 Open application shell

```http
GET /
```

Serves the Open OnDemand application shell. The backend injects `APP_URL` into the template so the frontend can understand its current base URL.

### Success message box

```text
Network application loaded successfully.
```

### Failure message box

```text
Could not load the Network application. Check the backend configuration and Open OnDemand app path.
```

### Success response example

```html
<!-- index.html rendered by Flask -->
```

### Failure response example

```html
<!-- error.html rendered by Flask -->
```

---

## 7.3 List networks

```http
GET /api/v1/networks
```

Returns all networks available from the Luna2 daemon.

Internally calls:

```text
GET <luna-daemon>/config/network
```

### Success message box

```text
Networks loaded successfully.
```

### Failure message box

```text
Could not load networks. Check the Luna daemon connection and API credentials.
```

### Success response example

```json
{
  "status": true,
  "status_code": 200,
  "content": {
    "config": {
      "network": {
        "cluster": {
          "name": "cluster",
          "network": "10.141.0.0/16",
          "type": "ethernet",
          "zone": "internal",
          "dhcp": true,
          "dhcp_range_begin": "10.141.128.0",
          "dhcp_range_end": "10.141.140.0",
          "gateway": "10.141.255.254",
          "gateway_metric": 101
        },
        "ib": {
          "name": "ib",
          "network": "10.149.0.0/16",
          "type": "infiniband",
          "zone": "internal",
          "dhcp": false
        }
      }
    }
  }
}
```

### Failure response example

```json
{
  "status": false,
  "status_code": 400,
  "content": "Request Timeout while https://<luna-daemon>/config/network"
}
```

---

## 7.4 Add network

```http
POST /api/v1/add
```

Creates a new network record in Luna2.

Internally calls:

```text
POST <luna-daemon>/config/network/<network-name>
```

The backend derives `<network-name>` from the first key below `config.network`.

### Success message box

```text
Network added successfully.
```

### Failure message box

```text
Could not add network. Check the network name, CIDR, DHCP range and Luna daemon validation message.
```

### Request body example

```json
{
  "config": {
    "network": {
      "newcluster": {
        "name": "newcluster",
        "network": "10.181.0.0/16",
        "type": "ethernet",
        "zone": "internal",
        "dhcp": true,
        "dhcp_nodes_in_pool": false,
        "dhcp_nodes_only": false,
        "dhcp_range_begin": "10.181.128.0",
        "dhcp_range_end": "10.181.140.0",
        "gateway": "10.181.255.254",
        "gateway_metric": 101
      }
    }
  }
}
```

### Success response example

```json
{
  "status": true,
  "status_code": 201,
  "content": {
    "config": {
      "network": {
        "newcluster": {
          "name": "newcluster",
          "network": "10.181.0.0/16"
        }
      }
    }
  }
}
```

### Failure response examples

No JSON payload:

```json
{
  "status": false,
  "status_code": 400,
  "message": "No JSON payload received"
}
```

Daemon validation error:

```json
{
  "status": false,
  "status_code": 400,
  "content": {
    "message": "Invalid request: network newcluster could not be created"
  }
}
```

---

## 7.5 Update network

```http
PUT /api/v1/update
```

Updates an existing network record in Luna2.

Internally calls:

```text
POST <luna-daemon>/config/network/<network-name>
```

The backend expects the Luna daemon to return HTTP `204` for a successful update.

### Success message box

```text
Network updated successfully.
```

### Failure message box

```text
Could not update network. Check whether the network exists and whether the submitted values are valid.
```

### Request body example

```json
{
  "config": {
    "network": {
      "cluster": {
        "name": "cluster",
        "network": "10.141.0.0/16",
        "type": "ethernet",
        "zone": "internal",
        "dhcp": true,
        "dhcp_range_begin": "10.141.128.0",
        "dhcp_range_end": "10.141.140.0",
        "gateway": "10.141.255.254",
        "gateway_metric": 101
      }
    }
  }
}
```

### Success response example

```json
{
  "status": true,
  "status_code": 204,
  "content": {
    "message": "Network cluster is updated successfully."
  }
}
```

### Failure response examples

No JSON payload:

```json
{
  "status": false,
  "status_code": 400,
  "message": "No JSON payload received"
}
```

Daemon validation error:

```json
{
  "status": false,
  "status_code": 404,
  "content": {
    "message": "Invalid request: network cluster does not exist"
  }
}
```

---

## 7.6 Rename network

```http
PATCH /api/v1/rename
```

Renames an existing network record.

Internally calls:

```text
POST <luna-daemon>/config/network/<old-network-name>
```

The old name is taken from the first key below `config.network`.

The new name must be provided as `newnetname` inside the old network object.

### Success message box

```text
Network renamed successfully.
```

### Failure message box

```text
Could not rename network. Provide both the current network name and the new network name.
```

### Request body example

```json
{
  "config": {
    "network": {
      "cluster": {
        "newnetname": "renamedcluster"
      }
    }
  }
}
```

### Success response example

```json
{
  "status": true,
  "status_code": 200,
  "content": {
    "message": "Network cluster is renamed successfully."
  }
}
```

### Failure response examples

No JSON payload:

```json
{
  "status": false,
  "message": "No JSON payload received"
}
```

Missing current or new name:

```json
{
  "status": false,
  "status_code": 400,
  "content": "ERROR :: Record name and new name must be provided."
}
```

Daemon validation error:

```json
{
  "status": false,
  "status_code": 404,
  "content": {
    "message": "Invalid request: network cluster does not exist"
  }
}
```

---

## 7.7 Delete network

```http
DELETE /api/v1/delete/<record>
```

Deletes a network record from Luna2.

Internally calls:

```text
GET <luna-daemon>/config/network/<record>/_delete
```

### Path parameters

| Parameter | Required | Description |
| --- | --- | --- |
| `record` | Yes | Network name to delete. |

### Success message box

```text
Network deleted successfully.
```

### Failure message box

```text
Could not delete network. Check whether the network exists or is still used by nodes or reservations.
```

### Success response example

```json
{
  "status": true,
  "status_code": 204,
  "content": {
    "message": "Network cluster is deleted successfully."
  }
}
```

### Failure response example

```json
{
  "status": false,
  "status_code": 404,
  "content": {
    "message": "Invalid request: network cluster1 does not exist"
  }
}
```

---

## 7.8 Get IP information

```http
GET /api/v1/ipinfo/<network>/<ipaddress>
```

Returns the status or daemon-side information for one IP address inside one network.

Internally calls:

```text
GET <luna-daemon>/config/network/<network>/<ipaddress>
```

### Path parameters

| Parameter | Required | Description |
| --- | --- | --- |
| `network` | Yes | Luna network name. |
| `ipaddress` | Yes | IP address to inspect. |

### Success message box

```text
IP information loaded successfully.
```

### Failure message box

```text
Could not load IP information. Check the network name and IP address.
```

### Success response example

The exact `content` object depends on the Luna daemon response.

```json
{
  "status": true,
  "status_code": 200,
  "content": {
    "config": {
      "network": {
        "cluster": {
          "10.141.0.1": {
            "status": "<daemon-returned-status-or-object>"
          }
        }
      }
    }
  }
}
```

### Failure response examples

Missing path values are normally prevented by the route itself, but the backend still validates them:

```json
{
  "status": false,
  "content": "network and ipaddress are required"
}
```

Daemon validation error:

```json
{
  "status": false,
  "status_code": 404,
  "content": {
    "message": "Invalid request: IP address was not found in network cluster"
  }
}
```

---

## 7.9 Get next free IP

```http
GET /api/v1/nextip/<network>
```

Returns the next available IP address for the selected network.

Internally calls:

```text
GET <luna-daemon>/config/network/<network>/_nextfreeip
```

### Path parameters

| Parameter | Required | Description |
| --- | --- | --- |
| `network` | Yes | Network name to search for a free IP. |

### Success message box

```text
Next free IP address found.
```

### Failure message box

```text
No free IP address could be found for this network.
```

### Success response example

```json
{
  "content": {
    "config": {
      "network": {
        "cluster": {
          "nextip": "10.141.0.1"
        }
      }
    }
  },
  "status": true,
  "status_code": 200
}
```

### Failure response example

```json
{
  "content": {
    "message": "Invalid request: network cluster1 does not provide for any free IP address"
  },
  "status": false,
  "status_code": 404
}
```

---

## 7.10 Get reserved IP addresses

```http
GET /api/v1/reserve/<network>
```

Returns the reserved IP addresses or network members for the selected network.

Internally calls:

```text
GET <luna-daemon>/config/network/<network>/_member
```

### Path parameters

| Parameter | Required | Description |
| --- | --- | --- |
| `network` | Yes | Network name to inspect. |

### Success message box

```text
Reserved IP addresses loaded successfully.
```

### Failure message box

```text
Could not load reserved IP addresses for this network.
```

### Success response example

The exact `content` object depends on the Luna daemon response.

```json
{
  "status": true,
  "status_code": 200,
  "content": {
    "config": {
      "network": {
        "cluster": {
          "member": [
            "<daemon-returned-member-or-reservation>"
          ]
        }
      }
    }
  }
}
```

### Failure response example

```json
{
  "status": false,
  "status_code": 404,
  "content": {
    "message": "Invalid request: network cluster1 does not exist"
  }
}
```

---

## 7.11 Licence information

```http
GET /api/v1/license
```

Returns the application licence text if the licence file exists and is readable.

Unlike most other API routes, this endpoint returns a plain text or HTML string, not the standard JSON envelope.

### Success message box

```text
Licence information loaded successfully.
```

### Failure message box

```text
Licence information is not available at this moment.
```

### Success response example

```html
GNU GENERAL PUBLIC LICENSE<br />Version 3, 29 June 2007<br />...
```

### Failure response example

```text
LICENSE Information is not available at this moment.
```

---

# 8. Frontend handling guide

## 8.1 Standard JSON API handling

For most API endpoints, the Vue.js frontend should check the JSON body:

```js
const result = await apiCall()

if (result.data?.status === true) {
  showSuccess(result.data.content)
} else {
  showFailure(result.data?.content || 'Request failed')
}
```

Do not treat HTTP `200` as success by itself.

The backend may return:

```json
{
  "status": false,
  "status_code": 404,
  "content": {
    "message": "Invalid request: network cluster1 does not provide for any free IP address"
  }
}
```

with an outer Flask HTTP response of `200`.

## 8.2 Error message extraction

Recommended frontend message extraction:

```js
function getApiMessage(data, fallback = 'Request failed') {
  if (!data) return fallback

  if (typeof data.content === 'string') return data.content

  if (data.content?.message) return data.content.message

  if (data.message) return data.message

  return fallback
}
```

## 8.3 Suggested UI behaviour

| Action | After success | After failure |
| --- | --- | --- |
| List networks | Render table/cards | Show error box and retry button |
| Add network | Show success box, refresh network list | Keep form values and show daemon message |
| Update network | Show success box, refresh selected network | Keep edit form open |
| Rename network | Show success box, refresh network list | Keep rename dialog open |
| Delete network | Show success box, refresh network list | Keep item visible and show error |
| Next IP | Fill IP field or show next IP box | Show no-free-IP message |
| Reserved IPs | Render reservation/member list | Show empty/error state |
| Licence | Render licence text | Show fallback message |

---

# 9. cURL examples

## List routes

```bash
curl -k https://vmware-controller1.cluster:7755/api/v1/routes
```

## List networks

```bash
curl -k https://vmware-controller1.cluster:7755/api/v1/networks
```

## Add network

```bash
curl -k -X POST https://vmware-controller1.cluster:7755/api/v1/add \
  -H 'Content-Type: application/json' \
  -d '{
    "config": {
      "network": {
        "newcluster": {
          "name": "newcluster",
          "network": "10.181.0.0/16",
          "type": "ethernet",
          "zone": "internal",
          "dhcp": true,
          "dhcp_range_begin": "10.181.128.0",
          "dhcp_range_end": "10.181.140.0",
          "gateway": "10.181.255.254",
          "gateway_metric": 101
        }
      }
    }
  }'
```

## Update network

```bash
curl -k -X PUT https://vmware-controller1.cluster:7755/api/v1/update \
  -H 'Content-Type: application/json' \
  -d '{
    "config": {
      "network": {
        "cluster": {
          "name": "cluster",
          "network": "10.141.0.0/16",
          "type": "ethernet",
          "zone": "internal",
          "dhcp": true,
          "dhcp_range_begin": "10.141.128.0",
          "dhcp_range_end": "10.141.140.0",
          "gateway": "10.141.255.254",
          "gateway_metric": 101
        }
      }
    }
  }'
```

## Rename network

```bash
curl -k -X PATCH https://vmware-controller1.cluster:7755/api/v1/rename \
  -H 'Content-Type: application/json' \
  -d '{
    "config": {
      "network": {
        "cluster": {
          "newnetname": "renamedcluster"
        }
      }
    }
  }'
```

## Delete network

```bash
curl -k -X DELETE https://vmware-controller1.cluster:7755/api/v1/delete/renamedcluster
```

## Get IP information

```bash
curl -k https://vmware-controller1.cluster:7755/api/v1/ipinfo/cluster/10.141.0.1
```

## Get next free IP

```bash
curl -k https://vmware-controller1.cluster:7755/api/v1/nextip/cluster
```

## Get reserved IP addresses

```bash
curl -k https://vmware-controller1.cluster:7755/api/v1/reserve/cluster
```

## Get licence

```bash
curl -k https://vmware-controller1.cluster:7755/api/v1/license
```

---

# 10. Backend behaviour notes

These details matter for frontend implementation.

1. The backend is a wrapper around the Luna2 daemon. It does not store network data itself.
2. The backend retrieves daemon credentials from the Luna configuration file.
3. The backend requests a Luna daemon token and caches it in `TOKEN_FILE`.
4. If the cached token is invalid or expired, the backend requests a new token.
5. Create, update and rename requests all call the Luna daemon with `POST` internally.
6. Delete calls the Luna daemon using the daemon-specific `_delete` route.
7. The route method exposed to the frontend may differ from the method used internally against the Luna daemon.
8. Most API routes return JSON, but the application shell, licence route and configuration errors return HTML/text.
9. The frontend should refresh `/api/v1/networks` after add, update, rename or delete actions.
10. The frontend should show daemon validation messages directly where possible because they explain the real reason for failure.

---

# 11. Recommended frontend success and failure components

Every API action should expose two visible message areas.

## Success box

Use this for completed actions.

```text
Title: Success
Body: <action-specific success message>
Style: green/positive state
Behaviour: auto-dismiss after a few seconds, except for destructive actions where keeping it visible can help confirmation
```

## Failure box

Use this for validation, daemon, configuration or connection errors.

```text
Title: Request failed
Body: <daemon message or backend fallback message>
Style: red/error state
Behaviour: keep visible until the user closes it or retries
```

The failure box should prefer messages in this order:

1. `content.message`
2. `content` when it is a string
3. `message`
4. generic fallback message

---

# 12. Current API route overview

| Method | Route | Purpose |
| --- | --- | --- |
| `GET` | `/` | Serve Open OnDemand application shell |
| `GET` | `/api/v1/routes` | List available backend routes |
| `GET` | `/api/v1/networks` | List all Luna2 networks |
| `POST` | `/api/v1/add` | Add a network |
| `PUT` | `/api/v1/update` | Update a network |
| `PATCH` | `/api/v1/rename` | Rename a network |
| `DELETE` | `/api/v1/delete/<record>` | Delete a network |
| `GET` | `/api/v1/ipinfo/<network>/<ipaddress>` | Get information for one IP address |
| `GET` | `/api/v1/nextip/<network>` | Get next free IP address |
| `GET` | `/api/v1/reserve/<network>` | Get reserved IP addresses / members |
| `GET` | `/api/v1/license` | Return licence information |

---

# 13. Known limitations and improvement points

These are not blockers, but they are useful to know before connecting the Vue.js frontend.

- Some error responses are JSON, while others are HTML/text.
- Some Flask routes return HTTP `200` even when the body contains an error status.
- `rename` returns a missing-payload error without `status_code`.
- `404` pages are rendered as `error.html` with HTTP `200`.
- Create, update and rename assume one network object per request.
- The backend currently trusts the payload shape and passes validation mainly to the Luna daemon.

For a cleaner frontend contract later, consider standardising all API errors as JSON under the same envelope:

```json
{
  "status": false,
  "status_code": 400,
  "content": {
    "message": "Human-readable error message"
  }
}
```

---

# 14. Licence

This backend is part of the TrinityX software suite and is distributed under the GNU General Public License.

See the project licence file for full licence terms.
