# Flask Group app on the VM (Vite dev server proxies /api, /edit, … here).
# Change the host if yours differs. Use https only if app.py runs with ssl_context on the VM.
VITE_GROUP_BACKEND=https://yixin3-dev-ctrl001:7755
